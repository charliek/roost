# crates/

Rust workspace for `roost-core` (the daemon), `roost-cli` (shell-integration CLI, transitionally `roost-cli-rs`), `roost-proto` (generated bindings), and the Linux UI's supporting crates. Cargo workspace root lands here in Phase 2.

See [../docs/development/vision.md](../docs/development/vision.md) for the target architecture and phased path. Empty for now — Phase 0 placeholder.
