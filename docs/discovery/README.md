# Discovery notes

Working notes, not product commitments. `mkdocs.yml` lists this
folder under `exclude_docs`, so it is not in the nav and is not
built into the public site.

| Note | What it is |
|---|---|
| [host-sessions.md](host-sessions.md) | Opt-in persist + SSH: `roost-session`, iced as a smart libghostty client, Superlogical-shaped attach. |
| [agent-watching.md](agent-watching.md) | Expanding agent coverage (hooks + screen/process fallback) without blocking host sessions. |

Update the notes when a later pass changes a recommendation. Implementation plans and `docs/development/vision.md` decision-log edits belong in the PR that actually builds the feature.
